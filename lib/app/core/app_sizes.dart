import 'package:flutter/material.dart';

class AppSizes {
  AppSizes._();

  static const spacing2px = 2.0;
  static const spacing4px = 4.0;
  static const spacing8px = 8.0;
  static const spacing16px = 16.0;
  static const spacing32px = 32.0;
  static const spacing64px = 64.0;
  static const spacing80px = 80.0;
  static const spacing128px = 128.0;

  static final borderRadius2px = BorderRadius.circular(2);
  static final borderRadius4px = BorderRadius.circular(4);
  static final borderRadius8px = BorderRadius.circular(8);
  static final borderRadius16px = BorderRadius.circular(16);
  static final borderRadius32px = BorderRadius.circular(32);
}
